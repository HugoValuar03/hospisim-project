@model Hospisim.Models.Exame

@{
    ViewData["Title"] = "Excluir Exame";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir permanentemente o registro do exame abaixo? Esta ação não pode ser desfeita.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Exame: @Model.TipoExame</h4>
                <p class="mb-0">Paciente: @Model.Atendimento?.Paciente?.NomeCompleto</p>
            </div>
            <div class="card-body">
                <p><strong>Data da Solicitação:</strong> @Model.DataSolicitacao.ToShortDateString()</p>
                <p><strong>Resultado:</strong> @Model.Resultado</p>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Exame" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>