@model Hospisim.Models.Prescricao

@{
    ViewData["Title"] = "Excluir Prescrição";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir permanentemente a prescrição abaixo? Esta ação não pode ser desfeita.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Prescrição para: @Model.Atendimento?.Paciente?.NomeCompleto</h4>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>Medicamento:</strong> @Model.Medicamento</p>
                        <p><strong>Dosagem:</strong> @Model.Dosagem</p>
                        <p><strong>Frequência:</strong> @Model.Frequencia</p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>Profissional Responsável:</strong> @Model.ProfissionalSaude?.NomeCompleto</p>
                        <p><strong>Data de Início:</strong> @Model.DataInicio.ToShortDateString()</p>
                        <p><strong>Status:</strong> @Model.StatusPrescricao</p>
                    </div>
                </div>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Prescrição" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>