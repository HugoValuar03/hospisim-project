@model Hospisim.Models.Internacao

@{
    ViewData["Title"] = "Excluir Internação";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir permanentemente o registro de internação abaixo? Esta ação não pode ser desfeita.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Paciente: @Model.Paciente?.NomeCompleto</h4>
            </div>
            <div class="card-body">
                <p><strong>Data de Entrada:</strong> @Model.DataEntrada.ToShortDateString()</p>
                <p><strong>Motivo da Internação:</strong> @Model.MotivoInternacao</p>
                <p><strong>Setor:</strong> @Model.Setor</p>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Internação" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>