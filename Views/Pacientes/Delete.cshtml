@model Hospisim.Models.Paciente

@{
    ViewData["Title"] = "Excluir Paciente";
}

<div class="row justify-content-center">
    <div class="col-md-10">

        <h1 class="text-center">@ViewData["Title"]</h1>
        <h3 class="text-center text-danger">Você tem certeza que deseja excluir este registro?</h3>

        <div asp-validation-summary="All" class="text-danger mt-3"></div>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Esta ação é permanente e não pode ser desfeita. Ao confirmar, todos os dados do paciente abaixo serão removidos do sistema.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">@Model.NomeCompleto</h4>
                <p class="mb-0">CPF: @Model.Cpf</p>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h5>Dados Pessoais</h5>
                        <p><strong>Data de Nascimento:</strong> @Model.DataNascimento.ToString("dd/MM/yyyy")</p>
                        <p><strong>Sexo:</strong> @Model.Sexo</p>
                        <p><strong>Tipo Sanguíneo:</strong> @Model.TipoSanguineo</p>
                    </div>
                    <div class="col-md-6">
                        <h5>Contato e Convênio</h5>
                        <p><strong>Telefone:</strong> @Model.Telefone</p>
                        <p><strong>Nº Cartão SUS:</strong> @Model.NumeroCartaoSus</p>
                        <p><strong>Possui Plano de Saúde:</strong> @(Model.PossuiPlanoSaude ? "Sim" : "Não")</p>
                    </div>
                </div>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Permanentemente" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>

    </div>
</div>