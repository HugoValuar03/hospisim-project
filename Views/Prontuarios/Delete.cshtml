@model Hospisim.Models.Prontuario

@{
    ViewData["Title"] = "Excluir Prontuário";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir o prontuário abaixo? Esta ação não pode ser desfeita e pode afetar atendimentos vinculados.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Prontuário Nº: @Model.NumeroProntuario</h4>
            </div>
            <div class="card-body">
                <p><strong>Paciente:</strong> @Model.Paciente?.NomeCompleto</p>
                <p><strong>Data de Abertura:</strong> @Model.DataAbertura.ToShortDateString()</p>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="NumeroProntuario" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Prontuário" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>