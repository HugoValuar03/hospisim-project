@model IEnumerable<Hospisim.Models.Prontuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Prontuário</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataAbertura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ObservacoesGerais)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paciente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataAbertura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ObservacoesGerais)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paciente.Cpf)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumeroProntuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumeroProntuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumeroProntuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
