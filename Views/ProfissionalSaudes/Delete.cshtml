@model Hospisim.Models.ProfissionalSaude

@{
    ViewData["Title"] = "Excluir Profissional";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir permanentemente o registro de <strong>@Model.NomeCompleto</strong>? Esta ação não pode ser desfeita e pode afetar registros de atendimentos e prescrições.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Dados do Profissional a ser Excluído</h4>
            </div>
            <div class="card-body">
                <p><strong>Nome Completo:</strong> @Model.NomeCompleto</p>
                <p><strong>CPF:</strong> @Model.Cpf</p>
                <p><strong>Registro:</strong> @Model.TipoRegistro - @Model.RegistroConselho</p>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Profissional" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>