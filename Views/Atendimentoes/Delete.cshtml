@model Hospisim.Models.Atendimento

@{
    ViewData["Title"] = "Excluir Atendimento";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Atenção!</h4>
            <p>Você tem certeza que deseja excluir permanentemente o registro do atendimento abaixo? Esta ação não pode ser desfeita.</p>
        </div>

        <div class="card shadow-sm mt-4">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Atendimento de: @Model.Paciente?.NomeCompleto</h4>
                <p class="mb-0">Realizado em: @Model.DataEHora.ToString("g")</p>
            </div>
            <div class="card-body">
                <p><strong>Profissional de Saúde:</strong> @Model.ProfissionalSaude?.NomeCompleto</p>
                <p><strong>Tipo de Atendimento:</strong> @Html.DisplayFor(model => model.Tipo)</p>
                <p><strong>Status:</strong> @Html.DisplayFor(model => model.Status)</p>
                <p><strong>Local:</strong> @Model.Local</p>
            </div>
        </div>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <input type="submit" value="Sim, Excluir Atendimento" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Não, Voltar para a Lista</a>
            </div>
        </form>
    </div>
</div>